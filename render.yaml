services:
  - type: web
    name: credit-card-approval-prediction
    env: python
    rootDir: 5. Project Development Phase
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.3
